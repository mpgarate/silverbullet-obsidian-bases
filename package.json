{
  "name": "silverbullet-bases",
  "version": "0.1.0",
  "description": "SilverBullet plug for viewing Obsidian Bases .base files.",
  "license": "MIT",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm run generate-viewer && npx plug-compile silverbullet-bases.plug.yaml",
    "generate-viewer": "node scripts/generate-viewer.mjs",
    "test": "node --test"
  },
  "devDependencies": {
    "@silverbulletmd/silverbullet": "^2.6.1"
  }
}
